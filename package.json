{
  "name": "80s-nightmare-band-site",
  "version": "1.0.0",
  "private": true,
  "description": "Official website for A Nightmare on 80s Street",
  "scripts": {
    "dev": "wrangler pages dev public",
    "qa": "node scripts/qa.mjs",
    "deploy": "npm run qa && wrangler pages deploy public --project-name 80s-nightmare-band"
  },
  "devDependencies": {
    "wrangler": "^4.28.1"
  }
}
